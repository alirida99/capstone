import * as React from 'react';
import HomeAuthComponent from '../../src/components/main/homeAuth';

interface HomeAuthPageProps { }

const HomeAuthPage: React.FC<HomeAuthPageProps> = () => {
    return (
        <HomeAuthComponent />
    );
}

export default HomeAuthPage;
